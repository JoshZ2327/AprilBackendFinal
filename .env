# Solana RPC Configuration
RPC_URL=https://api.mainnet-beta.solana.com

# Jupiter Aggregator API
JUPITER_API=https://quote-api.jup.ag/v4/quote

# Trading Configuration
DEFAULT_GAS_RESERVE=0.01          # Default SOL reserve for gas fees
STOP_LOSS_PERCENTAGE=0.01         # Stop-loss percentage (e.g., 1%)
TRADE_AMOUNT_PERCENTAGE=5         # Percentage of portfolio to allocate per trade

# Notifications (Optional)
NOTIFICATIONS_ENABLED=true        # Enable/disable notifications (true/false)
NOTIFICATIONS_METHOD=both         # Options: email, sms, both

# API Keys (Optional for Authentication or External Services)
API_KEY=your_api_key_here         # Replace with an API key if required
